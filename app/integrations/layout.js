export const metadata = {
  title: "Integrations | FeedbackSpec",
  description: "Setup integrations to automatically collect feedback",
};

export default function IntegrationsLayout({ children }) {
  return children;
}